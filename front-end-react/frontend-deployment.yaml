apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydeployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: front-end-pod
  template:
    metadata:
      labels:
        app: front-end-pod
    spec:
      containers:
      - name: frontend
        image: cicd-project-frontend:latest
        ports:
        - containerPort: 3000